---
title: "SPAR: Semantic-Pixel Self-Alignment and Adaptive Routing for Unified Multimodal Models"
title_zh: SPAR：面向统一多模态模型的语义-像素自对齐与自适应路由
authors: "Hongxiang Li, Hongxu Chen, Chenyang Zhu, Xiaoshuang Huang, Jiayin Cai, Xiaolong Jiang, Yao Hu, Long Chen"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23041v1"
tags: ["query:um"]
score: 10.0
evidence: 直接提出统一多模态模型框架
tldr: 多模态大模型在视觉理解上表现优异，但因语义感知与像素级重建的特征差异，在视觉生成方面受限。为此，提出SPAR框架，包含非对称双流统一分词器（语义流+Transformer增强像素流）以调和特征差异，并提出自对齐生成范式利用该分词器作为内部对齐教师引导扩散模型，同时引入动态令牌路由自适应聚合多层MLLM特征。实验表明，SPAR在统一架构中达到SOTA，生成与重建质量优异，且不损失基础理解能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23041v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1208, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1268, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1241, \"height\": 694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1263, \"height\": 1247, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23041v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 751, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 827, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1222, \"height\": 848, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1221, \"height\": 590, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1198, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 957, \"height\": 221, \"label\": \"Table\"}]"
motivation: 现有MLLMs因语义与像素特征差异导致视觉生成性能受限，需弥合这一差距以实现统一的视觉理解与生成。
method: 提出SPAR，含非对称双流分词器调和语义与像素，自对齐生成范式利用内部分词器引导扩散，及动态令牌路由自适应融合多层特征。
result: 在统一架构上达到SOTA，生成与重建质量显著提升，同时保持基础的视觉理解能力。
conclusion: SPAR有效统一了视觉理解与生成，为多模态模型提供了一种无需外部教师的自对齐新范式。
---

## 摘要
多模态大语言模型（MLLMs）在视觉理解方面取得了显著成功，但由于语义感知与像素级重建之间的根本特征差异，其在视觉生成方面仍受到限制。弥合这一差距需要克服两个核心挑战：赋予语义编码器高保真重建能力，以及在不依赖外部教师的情况下有效将生成模型与语义空间对齐。为此，我们提出了一种新颖的统一多模态框架，具有语义-像素自对齐与自适应路由（SPAR）。首先，为了调和语义感知与像素级重建，我们引入了一个非对称双流统一分词器。轻量级语义流锚定判别性特征，而Transformer增强的像素流将细粒度视觉细节恢复为统一的紧凑潜在空间。其次，为了消除外部依赖，我们提出了一种自对齐生成范式，该范式原生地利用这个优化的分词器作为扩散模型的内部对齐教师。此外，为了在该统一空间内促进灵活的多模态交互，我们引入了动态令牌路由，使每个令牌能够根据其不同的语义需求自适应地聚合多层MLLM特征。大量实验表明，SPAR建立了统一架构的最新水平，在保持基础视觉理解能力的同时，实现了卓越的生成和重建质量。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable success in visual understanding but remain constrained in visual generation due to the fundamental feature discrepancy between semantic perception and pixel-level reconstruction. Bridging this gap requires overcoming two core challenges: endowing semantic encoders with high-fidelity reconstruction capabilities, and effectively aligning generative models with semantic spaces without relying on external teachers. To this end, we propose a novel unified multimodal framework featuring \textbf{S}emantic-\textbf{P}ixel self-alignment and \textbf{A}daptive \textbf{R}outing (\textbf{SPAR}). First, to reconcile semantic perception with pixel-level reconstruction, we introduce an asymmetric dual-stream unified tokenizer. A lightweight semantic stream anchors discriminative features, while a Transformer-augmented pixel stream recovers fine-grained visual details into a unified compact latent space. Second, to eliminate external dependencies, we propose a self-aligned generation paradigm that natively leverages this optimized tokenizer as an internal alignment teacher for the diffusion model. Furthermore, to facilitate flexible multimodal interaction within this unified space, we introduce Dynamic Token Routing, which enables each token to adaptively aggregate multi-layer MLLM features based on its distinct semantic demands. Extensive experiments demonstrate that SPAR establishes the state-of-the-art for unified architectures, achieving exceptional generation and reconstruction quality while preserving foundational visual understanding capabilities.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多模态大语言模型（MLLMs）在视觉理解任务上表现优异，但在视觉生成方面严重受限。根本原因在于语义感知表征与像素级重建之间存在**特征差异**：语义编码器（如CLIP、DINOv2）通过判别性训练产生高度抽象的语义空间，丢失了大量高频纹理细节；而生成模型（如扩散模型）通常依赖于低维、紧凑的像素潜在空间（如VAE），导致两者难以直接统一。
- **整体含义**：论文旨在构建一个**统一的视觉理解与生成模型**，在不牺牲理解能力的前提下，赋予MLLMs高质量的图像生成、编辑能力。其关键挑战包括：（1）如何让语义编码器同时具备高保真像素重建能力而不引发灾难性遗忘；（2）如何在不依赖外部特征教师（如DINOv2）的情况下，将生成模型与语义空间自然对齐。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出SPAR框架，包含三个核心组件：
  - **非对称双流统一分词器（Asymmetric Dual-Stream Unified Tokenizer）**：将语义保留与像素重建显式解耦。包含：
    - **语义流（Semantic Stream）**：使用轻量级投影器（残差块+MLP）将冻结语义编码器的输出映射到紧凑潜在空间，作为语义锚点。
    - **像素流（Pixel Stream）**：使用Transformer编码器将语义特征转换为适合像素重建的中间表示，再投影到潜在空间，专门用于恢复高频细节。
    - **融合与解码**：两流输出通道拼接后通过融合层映射回潜在空间，输入预训练像素解码器（DC-AE）重建图像。
  - **自对齐生成范式（Self-Aligned Generation）**：利用上述优化后的统一分词器本身作为扩散模型（DiT）的内部对齐教师，无需外部特征网络。通过余弦相似度损失将对齐扩散模型中间特征与分词器编码器特征。
  - **动态令牌路由（Dynamic Token Routing, DTR）**：允许每个令牌根据自身语义角色，自适应地从MLLM的多层隐藏状态中聚合特征（通过轻量级路由网络计算每层权重），从而充分利用层次化表示。
- **关键技术细节**：
  - 分词器采用**渐进式三阶段训练**：阶段I冻结编码器与解码器，训练双流模块（使用像素对齐损失Lp）；阶段II冻结编码器，联合训练解码器；阶段III微调解码器，同时引入语义对齐损失Ls和GAN损失，以防遗忘。
  - 统一模型训练分三阶段：阶段I冻结MLLM和DiT，只训练连接器、查询嵌入和DTR；阶段II冻结MLLM，联合训练连接器与DiT，加入自对齐损失；阶段III监督微调。
  - DTR从MLLM的L层中均匀采样K层（默认4层），路由网络使用softmax计算权重，并引入可学习每层缩放参数α。

## 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法
- **数据集**：
  - 图像生成与编辑：BLIP3o的2700万重标注数据、CC12M的500万子集、JourneyDB的400万合成图像；编辑任务使用GPT-Image-Edit数据集；SFT阶段使用BLIP3o-60K和ShareGPT-4o-Image。
  - 理解任务：复用MLLM原生数据集，未额外训练（因LLM冻结）。
- **Benchmark**：
  - **图像重建**：ImageNet 50k验证集（256×256），指标：rFID、PSNR、SSIM。
  - **视觉理解**：MME-P、MMB、MMMU、MM-Vet、SEED、MMVP。
  - **文本到图像生成**：GenEval和WISE基准。
  - **图像编辑**：ImgEdit-Bench。
- **对比方法**：
  - 重建对比：LlamaGen、VAR、Open-MAGVIT2、RAE、SD-VAE、DC-AE、VA-VAE（生成式）；VILA-U、Tokenflow、DualViTok、DualToken、EMU2、UniLIP（统一式）。
  - 理解对比：LLaVA-OV、InternVL3、Qwen2.5-VL、Emu3-Chat（理解专用）；Chameleon、VILA-U、MetaMorph、SEED-X、Show-O、Janus-Pro、Harmon、BAGEL、BLIP3-o、TokLIP、Tar（统一式）。
  - 生成对比：SDXL、FLUX.1-dev、Emu3-Gen、SD3-Medium、SANA（生成式）；VILA-U、TokenFlow-XL、Janus-Pro、Harmon、BLIP3-o、BAGEL、OpenUni、Show-o2、Tar（统一式）。
  - 编辑对比：GPT-4o、MagicBrush、Instruct-P2P、AnyEdit、UltraEdit、OmniGen、Step1X-Edit、ICEdit、BAGEL、UniWorld-V1、OmniGen2。

## 4. 资源与算力
- **文中未明确说明**使用GPU型号、数量或训练时长。仅提及实验采用了两个模型变体（SPAR-1B和SPAR-3B），但未提供具体算力配置。需注意：这种缺失在一定程度上降低了实验的可复现性。

## 5. 实验数量与充分性
- **实验数量**：论文进行了多组实验，包括：
  - 图像重建对比（表1）：与7种生成式、6种统一式分词器对比。
  - 视觉理解对比（表2）：与6种理解专用、7种统一式模型对比（共13种）。
  - 文本到图像生成（表3）：与5种生成式、9种统一式模型对比。
  - 图像编辑（表4）：与11种方法对比。
  - 消融实验：统一分词器（表5）设4组（完整模型、去像素流、去语义流、冻结编码器）；统一模型（表6）设3组（完整、去DTR、去自对齐损失）。
- **充分性评估**：
  - **优点**：覆盖理解、生成、编辑三大任务，且包含多个权威benchmark；消融实验针对核心设计（双流、DTR、自对齐损失）进行验证；对比方法全面，包括生成专用与统一模型。
  - **不足**：消融实验仅各设3-4组，未探索超参数（如采样层数K、温度系数τ）的影响；未在更大规模模型（如7B级别）上验证；未见跨任务泛化性测试（如文本引导的图像修复或视频生成）。

## 6. 论文的主要结论与发现
- SPAR在统一架构上达到**新高度**：
  - 图像重建：rFID=0.27，PSNR=26.65，SSIM=0.856，显著优于现有统一分词器（如VILA-U、DualToken），甚至超过部分生成专用分词器（如VA-VAE在SSIM上领先）。
  - 视觉理解：在MME-P、MMB、MMMU等基准上超越InternVL3等纯理解基线，且无退化。
  - 文本到图像生成：GenEval总体0.91（SPAR-3B），超越所有对比方法，包括生成专用模型FLUX.1-dev（0.82）。
  - 图像编辑：ImgEdit-Bench总体4.01，仅次于闭源GPT-4o（4.20），开源最佳。
- **核心发现**：非对称双流设计成功解耦语义保持与像素重建，动态令牌路由和自对齐范式进一步提升了生成质量，验证了“统一分词器作为内部教师”的有效性。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：
  - 首次将**非对称双流**引入统一分词器，明确划分语义锚定与像素恢复职责，避免了传统单流强制兼容导致的性能折衷。
  - **自对齐生成范式**通过复用分词器作为内部教师，消除了对外部特征模型（如DINOv2）的依赖，减少了分布偏差。
  - **动态令牌路由（DTR）** 赋予每个令牌自适应选择多层MLLM特征的能力，相比固定最后一层的方法，更灵活地利用层次化表示。
- **实验设计亮点**：
  - 多任务、多基准评估（理解、生成、编辑），且与大量前沿方法对比，结果清单清晰。
  - 消融实验直接验证了每个核心组件的重要性（去像素流→PSNR下降2；去语义流→理解崩溃；去DTR/自对齐损失→生成指标下降）。
  - 提供可视化（图4）和路由权重分析，增强了方法的可解释性。

## 8. 不足与局限
- **实验覆盖有限**：
  - 未在更大规模模型（如7B+参数）上验证，结论的外推性存疑。
  - 缺少对**推理效率**（FLOPs、延迟）的评估，而DTR和双流结构可能引入额外计算开销。
  - 未在视频生成、3D生成等多模态扩展任务上测试。
- **偏差风险**：
  - 训练数据主要来自网络爬取和合成数据（JourneyDB、GPT-Image-Edit），可能存在**风格偏见**或**文化偏见**（论文表格中WISE基准的“Cultural”和“Biology”子项得分相对较低）。
  - 对比方法中部分模型的参数规模与SPAR不等（如FLUX.1-dev为12B），尽管SPAR在数倍更小参数下取得领先，但**公平性**受限于参数差异。
- **应用限制**：
  - 依赖MLLM的冻结特征，若基础MLLM（InternVL3）更新，需要重新训练连接器和DTR。
  - 图像编辑任务在“Ext.”（外部替换）子任务上得分较低（2.32），说明复杂语义编辑仍有挑战。
- **其他**：
  - 未提供开源代码、模型权重、训练超参数，降低了可复现性。
  - 资源与算力细节缺失，难以评估实际训练成本。

（完）
