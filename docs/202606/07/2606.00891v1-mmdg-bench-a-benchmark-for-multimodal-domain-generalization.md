---
title: "MMDG-Bench: A Benchmark for Multimodal Domain Generalization"
title_zh: MMDG-Bench：多模态领域泛化基准
authors: "Qianshan Zhan, Qian Wang, Da Li, Xiao-Jun Zeng, Xiatian Zhu"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00891v1"
tags: ["query:um"]
score: 8.0
evidence: 多模态域泛化的统一实验协议与框架
tldr: 多模态领域泛化（MMDG）利用互补模态提升模型在未见域上的鲁棒性，但当前研究缺乏统一基准和标准化评估。我们提出MMDG-Bench，包含D2M（先领域泛化后多模态学习）和M2D（先多模态学习后领域泛化）两种框架，覆盖动作识别和人脸防伪等任务。通过十个基线实验发现，集成领域泛化技术持续提升泛化能力，且框架选择取决于模态间稳定性：D2M适用于模态关系稳定的场景，M2D更适合跨域关系多变的情况。该基准为多模态鲁棒性研究提供了系统性评估平台和设计指南。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00891v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1253, \"height\": 1010, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00891v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 655, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00891v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 581, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00891v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 582, \"height\": 352, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00891v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1260, \"height\": 340, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00891v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1214, \"height\": 1558, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00891v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1305, \"height\": 669, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00891v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1253, \"height\": 603, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00891v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 591, \"height\": 417, \"label\": \"Table\"}]"
motivation: 现有MMDG研究局限于动作识别且缺乏标准化评估，需统一基准以系统比较不同集成策略。
method: 构建MMDG-Bench，定义D2M和M2D两种框架，配对统一多模态配置与五种领域泛化技术，在视频-音频-光流和RGB-深度-IR任务上实例化十个基线。
result: 结构化组合超越现有方法，DG技术提供稳定泛化增益，非DG方法对骨干敏感；D2M在稳定模态关系下更优，M2D应对跨域变化更鲁棒；更强骨干带来更大增益。
conclusion: MMDG-Bench提供原则性基准和设计指南，推动多模态鲁棒性研究的标准化与系统性进展。
---

## 摘要
多模态领域泛化（MMDG）旨在利用互补模态增强模型在未见领域上的鲁棒性。尽管多模态学习（MML）和领域泛化（DG）作为独立领域取得了广泛进展，但它们的系统整合仍未被充分探索。当前的MMDG研究主要局限于动作识别，且缺乏标准化的评估协议。为此，我们引入了MMDG-Bench，一个全面的基准，包含两个基础框架：先DG后MML（D2M）和先MML后DG（M2D）。我们在多种任务上提供了统一的实验协议，包括视频-音频-光流动作识别和RGB-深度-红外人脸反欺骗。通过将统一的MML配置与五种DG技术配对，在D2M和M2D两种顺序下实例化十个MMDG基线，我们证明这些结构化组合经常优于现有的最先进方法，强调了统一基准测试的必要性。我们的分析得出三个关键见解：（1）集成DG技术在不同骨干网络上提供一致的泛化提升，而非DG方法对骨干网络变化高度敏感；（2）最优框架选择取决于模态间稳定性：当模态关系在领域间稳定时，D2M表现优异，而M2D对跨领域关系变化更具鲁棒性；（3）更强的骨干网络在集成到我们的结构化框架中时会产生放大的性能收益。MMDG-Bench为未来多模态鲁棒性研究提供了原则性基础和可操作的设计指南。代码发布在https://github.com/qszhan/MMDG-Bench。

## Abstract
Multi-modal Domain Generalization (MMDG) seeks to leverage complementary modalities to enhance model robustness on unseen domains. Despite extensive progress in Multi-modal Learning (MML) and Domain Generalization (DG) as individual fields, their systematic integration remains under-explored. Current MMDG research is largely confined to action recognition and lacks standardized evaluation protocols. To address this, we introduce MMDG-Bench, a comprehensive benchmark featuring two foundational frameworks: DG then MML (D2M) and MML then DG (M2D). We provide unified experimental protocols across diverse tasks, including video-audio-flow action recognition and RGB-Depth-IR face anti-spoofing. By instantiating ten MMDG baselines through pairing a unified MML configuration with five DG techniques under both D2M and M2D orderings, we demonstrate that these structured combinations frequently outperform existing state-of-the-art methods, underscoring the necessity of a unified benchmarking effort. Our analysis yields three key insights: (1) Integrating DG techniques provides consistent generalization gains across various backbones, whereas non-DG methods are highly sensitive to backbone shifts; (2) The optimal framework choice depends on inter-modal stability: D2M excels when modal relations are stable across domains, while M2D is more robust to cross-domain relational variance; (3) Stronger backbones yield amplified performance dividends when integrated into our structured frameworks. MMDG-Bench provides a principled foundation and actionable design guidelines for future research in multi-modal robustness. Code is released at https://github.com/qszhan/MMDG-Bench.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：多模态领域泛化（MMDG）旨在利用互补模态信息提升模型在未见目标域上的泛化能力，但现有研究存在严重不足：
    - 缺乏系统性：多模态学习（MML）和领域泛化（DG）各自发展，但很少被统一整合。
    - 任务局限：绝大多数MMDG工作仅聚焦于动作识别，缺少跨任务的标准评估协议。
    - 方法不全面：早期工作重视模态对齐而忽略DG文献；近期工作虽引入DG概念但未系统利用现有DG方法。
- **核心问题**：如何构建一个统一、可扩展的基准，系统评估和比较不同MML与DG集成策略在多任务上的表现，并给出可操作的设计指南。
- **整体含义**：MMDG-Bench填补了该领域标准基准缺失的空白，提供了两个互补框架（D2M和M2D）及10个系统化基线，证明结构化集成方法优于现有分散设计，并揭示了框架选择与模态关系稳定性之间的关键联系。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将MMDG问题分解为两个有序步骤——“领域泛化（DG）”和“多模态学习（MML）”，提出两种互补框架：
    - **D2M (DG then MML)**：先对每个模态单独应用DG（如域对齐、数据增强等），使各模态特征域不变，再进行跨模态融合。适用于模态间关系在域间稳定场景。
    - **M2D (MML then DG)**：先在各源域内完成模态对齐与融合，再对融合后的多模态特征施加DG正则化。适用于模态间关系随域变化场景。
- **关键技术细节**：
    - **MML组件**（固定统一配置）：包括
        - 监督对比学习（跨模态特征对齐）
        - 模态翻译（跨模态重建，增强一致性）
        - 梯度混合（Gradient Blending，自适应调整模态梯度权重，防止模态主导）
        - 注意力瓶颈融合（Attention Bottleneck Fusion，压缩跨模态交互信息）
    - **DG组件**（5种）：ERM（基线）、Mixup（数据增强）、MMD（分布匹配）、CL（对比学习域对齐）、MIRO（互信息正则化）。所有方法使用SAM优化器提升泛化。
    - **框架实现**：图文详述了特征提取（模态特定编码器，部分冻结高层可训）、MML损失（翻译损失$L_{mt}$、对比损失$L_{mc}$）、DG正则化（$L_{mix}, L_{mmd}, L_{dc}, L_{MIRO}$）、最终分类损失（$L_{ERM}$）。
    - **目标函数**：$\min_f \sum_{k=1}^K \mathbb{E}_{(x,y)\sim D_s^{(k)}}[\ell(f(x),y)] + \lambda_d \Omega_{DG}(\{z^{(k)}\}) + \lambda_m \Omega_{MML}(\{z_m\})$，其中$\Omega_{DG}$和$\Omega_{MML}$分别强制域不变性和模态不变性。
    - **MMDG变体**：固定MML全部四个组件为MM，与5种DG方法配对，形成5种配置（MM+ERM, MM+Mixup, MM+MMD, MM+CL, MM+MIRO），分别在D2M和M2D下实例化，共10个基线。

#### 3. 实验设计：数据集、基准、对比方法
- **数据集与任务**：
    - **动作识别**：EPIC-Kitchens（8种动作，3个厨房作为3个域）、HAC（人类-动物-卡通，7种动作，3个域）。模态：视频、光流、音频。
    - **人脸反欺骗 (FAS)**：CASIA-CeFA、CASIA-SURF、WMCA（各为1个域）。模态：RGB、深度、红外。评估指标：HTER（越低越好）和AUC（越高越好）。
- **基准设置**：采用留一域评估（leave-one-domain-out），即用两个源域训练，在第三个未见域测试。动作识别上有6个任务（每个数据集3个域→域组合），FAS有3个任务。
- **对比方法**：
    - 基础基线：DeepAll（无DG）
    - 动作识别MMDG方法：SimMMDG, CMRF, MDJAT, MAD-DG（均复现）
    - FAS方法：ViT-Base-CA, mmdg（复现）
    - 本文提出的10种MMDG变体（D2M和M2D各5种）
- **骨干网络**：动作识别中对比CNN（SlowFast+ResNet-18+SlowFast-only）和ViT（VideoMAEv2+AST）；FAS中统一用ViT-Base（ImageNet预训练）。

#### 4. 资源与算力
- **动作识别实验**：单张NVIDIA H100（80 GB）GPU。CNN版本每任务约1小时，ViT版本约5小时。
- **FAS实验**：单张NVIDIA H100（80 GB）GPU，每实验需6–9小时（30轮训练）。
- **优化器**：SAM（Sharpness-Aware Minimization）作为默认，与AdamW、SGD对比。学习率、权重衰减等超参数已详细列出。
- **注**：论文明确给出了GPU型号和时长，算力描述充分。

#### 5. 实验数量与充分性
- **实验组数**：
    - 主实验：动作识别表1（2骨干×2数据集×3子任务×10方法 ≈ 120组结果），FAS表2（3子任务×10方法 ≈ 30组）。
    - 消融实验：图2a（MM组件消融，2数据集×2骨干×4配置）、图2b（优化器比较）、表3（缺失模态鲁棒性，3种缺失情况×10方法）、图4（超参数敏感性）、图3（骨干层解冻深度）、表4（跨模态关系稳定性CMRS分析）。
    - 此外，补充材料中还有两模态组合实验、每子任务详细结果等。
- **充分性评价**：
    - 充分：覆盖动作识别和FAS两个完全不同任务，CNN与ViT两种骨干，全模态与缺失模态设置，消融了MML组件、DG权重、优化器、骨干层深度，并引入CMRS指标解释框架选择。
    - 公平：所有对比方法均在统一骨干、优化器、数据划分下复现，避免实现差异带来的偏差。
    - 客观：结论有充分数据支撑，且讨论了框架选择的条件（基于CMRS定量指标），而非仅凭经验。

#### 6. 论文的主要结论与发现
1. **集成DG是关键**：MML+DG变体在所有任务上一致超越仅依赖MML的基线，且DG稳定了骨干升级带来的性能波动（ViT上非DG方法在EPIC-Kitchens退化，而DG方法持续提升）。
2. **框架选择取决于跨模态关系稳定性**：
    - D2M在模态关系域间稳定时更优（如EPIC-Kitchens：不同厨房共享相似跨模态语义，CMRS高且均匀）。
    - M2D在跨模态关系域间变化时更鲁棒（如HAC：人/动物/卡通音频差异大，CMRS低且不均匀；FAS：CMRS_bin低，M2D全面领先）。
3. **更强骨干放大框架对比差异**：ViT相比CNN提升了整体准确率，同时扩大了D2M与M2D的性能差距，使框架选择更关键。
4. **SAM优化器最稳定**：相比AdamW和SGD，SAM在两种框架、两种骨干下表现一致且最优。
5. **缺失模态鲁棒性**：M2D在缺失模态条件下优于D2M，因其鼓励模态发展语义完整、互补的表示，即使部分模态缺失仍能保持性能。

#### 7. 优点：方法或实验设计上的亮点
- **结构清晰的首个系统基准**：提出D2M和M2D两种通用框架，将复杂MMDG问题拆解为可组合的模块，便于扩展新MML或DG方法。
- **跨任务验证**：覆盖动作识别和FAS两种本质不同任务（多类分类 vs 二分类、时序 vs 图像），增强了结论泛化性。
- **定量解释框架选择**：引入CMRS（跨模态关系稳定性）指标，用数据支持框架选择策略，避免主观经验。
- **公平部署**：所有对比方法使用相同骨干、优化器、超参搜索范围，且开源代码。
- **全面消融**：从组件到超参数、优化器、骨干深度，分析细致，提供了实用指南（如推荐冻结大部分骨干层，L=1或2效果最好）。
- **缺失模态鲁棒性分析**：考虑实际应用场景，评估了传感器故障情况，使结论更具落地价值。

#### 8. 不足与局限
- **任务覆盖有限**：虽涵盖动作识别和FAS，仍缺少其他常见多模态任务（如视觉问答、情感识别、医学图像融合等），可能影响结论普适性。
- **MML配置固定**：所有变体使用统一MML组合（四项全部启用），未探索不同MML子集或顺序（如仅用对比+融合，无翻译）。虽做了消融，但未与DG组件交互消融。
- **DG方法选择有限**：仅纳入5种代表性DG方法，未涵盖近年更优方法（如Fishr、SelfReg、Causality等），可能导致“最佳”变体非最优。
- **CMRS指标计算依赖特征提取器**：使用冻结骨干提取特征计算CMRS，若骨干本身未充分训练或模态维度差异大（如音频与视频），相关性度量可能不够稳定（论文使用了CCA式相关和Spearman相关系数，但未讨论鲁棒性）。
- **框架选择仅依赖CMRS**：实际中可能还需考虑模态缺失、计算成本等因素。论文仅基于稳定性给出建议，未提供多目标权衡。
- **计算资源偏高**：ViT版本需5小时/任务，FAS需6-9小时，对资源受限团队复制门槛较高。但考虑到是基准工作，可接受。
- **未对比最新M2D/D2M类似方法**：如直接基于Transformer的端到端多模态域泛化方法（如ViLT+域对齐），论文仅在CNN/ViT骨干上实例化，未对比近期更强的融合架构（如Fusion-Transformer专用设计）。

（完）
